% Shared preamble for per-experiment standalone builds.
% Mirrors the ICLR template's measure (10pt, textwidth 5.5in) so that a table
% which fits here fits in the paper. The template's Times package is omitted:
% the XeTeX-based engine cannot load it, and no check depends on the font.
\usepackage[letterpaper,textwidth=5.5in,textheight=9in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{url}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
